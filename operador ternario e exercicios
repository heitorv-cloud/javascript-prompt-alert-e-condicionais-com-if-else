/*alert('boas vindas ao jogo do mundo secreto');
let numeroSecreto= parseInt(Math.random() * 100 + 1);
console.log(numeroSecreto);
let chute; //fica vazio porque agora so vai funcionar pra armazenar e o while pede o valor
let tentativas = 1;



//while enquanto o chute for diferente do numero secreto, o jogo continua
while (chute != numeroSecreto)  {
    chute = prompt ('escolha um numero entre 0 e 100');

if(chute == numeroSecreto) {
  break; //para o jogo quando o chute for igual ao numero secreto

} else {
    if(chute > numeroSecreto) {
        alert(`o numero secreto é menor que ${chute}`);
    } else {
        alert(`o numero secreto é maior que ${chute}`);
    }
    tentativas++;    
}
}

//o alert precisou vir para ca porque o break para o jogo la em cima quando o jogador acerta

*/

/*if (tentativas > 1) {
    alert(`o numero secreto é ${numeroSecreto} e vc acertou com ${tentativas} tentativas`);
} else {
    alert(`o numero secreto é ${numeroSecreto} e vc acertou com ${tentativas} tentativa`);  
}
    // ao inves de fazer dessa forma podemos fazer usando operador ternario.


let palavratentativa = tentativas > 1 ? 'tentativas' : 'tentativa'; 
// operador ternario, se a condição for verdadeira ele retorna a primeira parte, se for falsa ele retorna a segunda parte
alert(`o numero secreto é ${numeroSecreto} e vc acertou com ${tentativas} ${palavratentativa}`);
*/

